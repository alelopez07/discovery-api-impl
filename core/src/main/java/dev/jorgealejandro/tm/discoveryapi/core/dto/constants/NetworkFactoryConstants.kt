package dev.jorgealejandro.tm.discoveryapi.core.dto.constants

enum class NetworkFactoryConstants(val content: String) {
    API_KEY(content = "apikey"),
    CONTENT_TYPE_KEY(content = "Content-Type"),
    ACCEPT_KEY(content = "Accept"),
    JSON_HEADER_TYPE_VALUE(content = "application/json")
}