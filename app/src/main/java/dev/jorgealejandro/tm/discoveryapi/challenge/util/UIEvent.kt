package dev.jorgealejandro.tm.discoveryapi.challenge.util

/**
 * [UIEvent]
 * Interface used to centralize the events that can be handled by a BaseViewModel or
 * some other origins.
 *
 * This interface is designed to be used with the Event classes, which ensures that events are
 * only handled once. By using this interface, you can easily define and handle events in a
 * consistent manner across your ViewModels.
 */
interface UIEvent